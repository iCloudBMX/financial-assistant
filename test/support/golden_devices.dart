import 'package:flutter/widgets.dart';

/// Narrow-phone golden canvases use an extended height so a complete component
/// gallery is captured in one deterministic image without scrolling.
const phone320 = Size(320, 1800);
const phone390 = Size(390, 1100);

const textScale200 = 2.0;
